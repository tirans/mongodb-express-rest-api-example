apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongo-app.fullname" . }}-test-connection"
  labels:
    {{- include "mongo-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongo-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
